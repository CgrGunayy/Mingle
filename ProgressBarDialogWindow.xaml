<Window x:Class="MingleWPF.ProgressBarDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:local="clr-namespace:MingleWPF"
        mc:Ignorable="d"
        WindowStyle="None"
        Title="ProgressBar" Height="180" Width="400" 
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        >

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">

                        <Border x:Name="ButonCercevesi" 
                    Background="{TemplateBinding Background}" 
                    BorderThickness="{TemplateBinding BorderThickness}" 
                    CornerRadius="4">

                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
          Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButonCercevesi" Property="Background" Value="#333333"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButonCercevesi" Property="Background" Value="#555555"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButonCercevesi" Property="Background" Value="#1A1A1A"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#1A1A1A" CornerRadius="15" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Direction="270" Opacity="0.6" Color="Black"/>
        </Border.Effect>

        <Grid Background="#1A1A1A">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="2"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="30"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" >
                    <Border.Background>
                        <LinearGradientBrush EndPoint="1,-0.4" StartPoint="0,0">
                            <GradientStop Color="#00A4133C" Offset="0"/>
                            <GradientStop Color="#FFA4133C" Offset="0.5"/>
                            <GradientStop Color="#00A4133C" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>

                <Grid Grid.Row="1" MouseDown="TopBar_MouseDown">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Margin="10, 0, 0, 2" Content="Exporting..." HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Regular" FontSize="24" Foreground="#E5E2E1"/>
                    <Button Grid.Column="1" Click="CloseButton_Click" Height="27" Background="#131313" BorderThickness="0" Margin="10, 0, 10, 0" HorizontalAlignment="Right">
                        <iconPacks:PackIconBootstrapIcons Kind="XSquare" Width="24" Height="24" Foreground="#A4133C"/>
                    </Button>
                </Grid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="20, 10, 20, 5">
                    <ProgressBar x:Name="RenderProgressBar" Width="275" Minimum="0" Maximum="100"  Background="#1A1A1A" Foreground="#00A2FF" BorderThickness="0"/>
                    <TextBlock x:Name="RenderProgressText" Margin="5, 0, 0, 0" Text="% 0.0" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>

                <Border  Grid.Row="3" >
                    <Label x:Name="RenderResultText" Margin="10, 0, 0, 2" Visibility="Hidden" Content="Export Completed!" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Regular" FontSize="14" Foreground="#E5E2E1"/>
                </Border>
            </Grid>
        </Grid>

    </Border>
</Window>