<Window x:Class="MingleWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MingleWPF"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        FontFamily="./Fonts/#Manrope"
        Height="720" Width="1024" 
        WindowStyle="None"
        AllowsTransparency="True" 
        Background="Transparent">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">

                        <Border x:Name="ButonCercevesi" 
                            Background="{TemplateBinding Background}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="4">

                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                  Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButonCercevesi" Property="Background" Value="#333333"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButonCercevesi" Property="Background" Value="#555555"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButonCercevesi" Property="Background" Value="#1A1A1A"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TabButton" TargetType="Button">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">

                        <Border x:Name="ButonCercevesi" 
                    Background="{TemplateBinding Background}" 
                    BorderThickness="{TemplateBinding BorderThickness}" 
                    CornerRadius="4">

                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                  Margin="{TemplateBinding Padding}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButonCercevesi" Property="Background" Value="#19A4133C"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButonCercevesi" Property="Background" Value="#19A4133C"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButonCercevesi" Property="Background" Value="#1A1A1A"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="#131313"/>
            <Setter Property="Foreground" Value="#FFBDBDBD"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">

                        <Border x:Name="MenuArkaPlan" 
                            Background="{TemplateBinding Background}" 
                            BorderThickness="0">
                            <Grid>
                                <ContentPresenter ContentSource="Header" 
                                              HorizontalAlignment="Left" 
                                              VerticalAlignment="Center" 
                                              Margin="{TemplateBinding Padding}"/>

                                <Popup x:Name="Popup" 
                                   Placement="Bottom" 
                                   Width="125"
                                   IsOpen="{TemplateBinding IsSubmenuOpen}" 
                                   AllowsTransparency="True" 
                                   Focusable="False">
                                    <Border Background="#131313" BorderThickness="0">
                                        <StackPanel IsItemsHost="True" />
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="MenuArkaPlan" Property="Background" Value="#333333"/>
                            </Trigger>

                            <Trigger Property="IsSubmenuOpen" Value="True">
                                <Setter TargetName="MenuArkaPlan" Property="Background" Value="#555555"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#131313" MouseDown="TopBar_MouseDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="275"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="MINGLE" Foreground="#A4133C" FontSize="24" Padding="25, 0, 0, 0" HorizontalContentAlignment="Left" VerticalContentAlignment="Center" FontWeight="ExtraBold" />
            <Menu Grid.Column="1" Background="#131313" VerticalAlignment="Center">
                <MenuItem Header="FILE" FontSize="14" FontWeight="Bold">
                    <MenuItem Header="NEW"/>
                    <MenuItem Header="OPEN"/>
                    <MenuItem Header="SAVE"/>
                </MenuItem>
                <MenuItem Header="EDIT" FontSize="14" FontWeight="Bold">
                    <MenuItem Header="NEW"/>
                    <MenuItem Header="OPEN"/>
                    <MenuItem Header="SAVE"/>
                </MenuItem>
                <MenuItem Header="VIEW" FontSize="14" FontWeight="Bold">
                    <MenuItem Header="NEW"/>
                    <MenuItem Header="OPEN"/>
                    <MenuItem Header="SAVE"/>
                </MenuItem>
                <MenuItem Header="HELP" FontSize="14" FontWeight="Bold">
                    <MenuItem Header="NEW"/>
                    <MenuItem Header="OPEN"/>
                    <MenuItem Header="SAVE"/>
                </MenuItem>
            </Menu>
            <StackPanel Grid.Column="2" Margin="0, 0, 10, 0" VerticalAlignment="Center" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Click="MinimizeButton_Click" Background="#131313" BorderThickness="0" Margin="5, 0, 5, 0">
                    <iconPacks:PackIconBootstrapIcons Kind="DashSquare" Width="24" Height="24" Foreground="#A4133C"/>
                </Button>
                <Button Click="MaximizeButton_Click" Background="#131313" BorderThickness="0" Margin="5, 0, 5, 0">
                    <iconPacks:PackIconBootstrapIcons Kind="Square" Width="24" Height="24" Foreground="#A4133C"/>
                </Button>
                <Button Click="CloseButton_Click" Background="#131313" BorderThickness="0" Margin="5, 0, 5, 0">
                    <iconPacks:PackIconBootstrapIcons Kind="XSquare" Width="24" Height="24" Foreground="#A4133C"/>
                </Button>

            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Background="#20201F">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="2"/>
                <ColumnDefinition Width="0.7*"/>
            </Grid.ColumnDefinitions>

            <!-- Side Panel -->
            <Grid Grid.Column="0" Background="#20201F">
                <Grid.RowDefinitions>
                    <RowDefinition Height="100"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="130"/>
                </Grid.RowDefinitions>
            
                <Grid Grid.Row="0"> 
                    <Grid.RowDefinitions>
                        <RowDefinition Height="0.5*"/>
                        <RowDefinition Height="0.5*"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0" Margin="20, 0, 0, 0" Grid.RowSpan="1" Content="Project Name" VerticalContentAlignment="Bottom" FontWeight="Bold" FontSize="18" Foreground="#E5E2E1"/>
                    <Label Grid.Row="1" Margin="21, 0, 0, 0" Content="1080p" VerticalContentAlignment="Top" FontWeight="Thin" FontSize="12" Foreground="#FFE0BEC1"/>
                </Grid>

                <StackPanel Grid.Row="1">
                    <Button Height="50" Background="#20201F" Click="MediaButton_Click" Margin="10, 0, 10, 0" HorizontalContentAlignment="Left">

                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource TabButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentTab}" Value="{x:Static local:MingleTab.Media}">
                                        <Setter Property="Background" Value="#C62828"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconBootstrapIcons Margin="20, 0, 0, 0" Kind="FileImage" Foreground="#FFBDBDBD"  VerticalAlignment="Center"/>
                            <Label Content="Media" Margin="5, 0, 0, 0" Foreground="#FFBDBDBD" FontSize="14" FontWeight="Medium"  VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource TabButton}" Height="50" Background="#20201F" Margin="10, 0, 10, 0" HorizontalContentAlignment="Left">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconBootstrapIcons Margin="20, 0, 0, 0" Kind="LightningChargeFill" Foreground="#FFBDBDBD"  VerticalAlignment="Center"/>
                            <Label Content="Effects" Margin="5, 0, 0, 0" Foreground="#FFBDBDBD" FontSize="14" FontWeight="Medium"  VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource TabButton}" Height="50" Background="#20201F" Margin="10, 0, 10, 0" HorizontalContentAlignment="Left">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconBootstrapIcons Margin="20, 0, 0, 0" Kind="Soundwave" Foreground="#FFBDBDBD"  VerticalAlignment="Center"/>
                            <Label Content="Audio" Margin="5, 0, 0, 0" Foreground="#FFBDBDBD" FontSize="14" FontWeight="Medium"  VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Row="2">
                    <Button Height="50" Background="#353535" Margin="10, 0, 10, 0" HorizontalContentAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconBootstrapIcons Width="10" Height="10" Kind="Plus" Foreground="#FFBDBDBD"  VerticalAlignment="Center"/>
                            <Label Content="Import" Foreground="#FFBDBDBD" FontWeight="Bold" FontSize="14"  VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Height="50" Background="#20201F" Margin="10, 10, 10, 0" HorizontalContentAlignment="Left">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconBootstrapIcons Margin="20, 0, 0, 0" Kind="Gear" Foreground="#FFBDBDBD"  VerticalAlignment="Center"/>
                            <Label Content="Settings" Margin="5, 0, 0, 0" Foreground="#FFBDBDBD" FontSize="14" FontWeight="Medium"  VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <Border Grid.Column="1" Background="#19594043"/>

            <Grid Grid.Column="2" Background="#20201F">
                <Grid.RowDefinitions>
                    <RowDefinition Height="0.65*"/>
                    <RowDefinition Height="0.35*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.3*"/>
                        <ColumnDefinition Width="2"/>
                        <ColumnDefinition Width="0.7*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="70"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" VerticalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="48"/>
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="searchBox" Grid.Column="0" Height="35" KeyDown="SearchBox_KeyDown" VerticalContentAlignment="Center" MaxLines="1" Margin="20, 0, 0, 0">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

                                        <Setter Property="Background" Value="#1A1A1A"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="BorderBrush" Value="#333333"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="Padding" Value="2"/>
                                        <Setter Property="CaretBrush" Value="White"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="TextBox">
                                                    <Border x:Name="ArkaPlan" 
                                                        Background="{TemplateBinding Background}" 
                                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                                        BorderThickness="{TemplateBinding BorderThickness}" 
                                                        CornerRadius="4">
                                                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                                                    </Border>

                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="ArkaPlan" Property="BorderBrush" Value="#555555"/>
                                                        </Trigger>

                                                        <Trigger Property="IsKeyboardFocused" Value="True">
                                                            <Setter TargetName="ArkaPlan" Property="BorderThickness" Value="1.5"/>
                                                            <Setter TargetName="ArkaPlan" Property="Background" Value="#222222"/>
                                                        </Trigger>

                                                        <Trigger Property="IsEnabled" Value="False">
                                                            <Setter TargetName="ArkaPlan" Property="Background" Value="#0D0D0D"/>
                                                            <Setter Property="Foreground" Value="#666666"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                            <Button Grid.Column="1" Click="SearchButton_Click" Background="#20201F" Width="28" Height="28" HorizontalContentAlignment="Center">
                                    <iconPacks:PackIconBootstrapIcons  Width="16" Height="16" Kind="Search" VerticalAlignment="Center" HorizontalAlignment="Left" Foreground="#FFB2BA"/>
                            </Button>
                        </Grid>
                    </Grid>

                    <Border Grid.Column="1" Background="#19594043" />

                    <Border Grid.Column="2" Background="#1C1B1B">

                    </Border>
                </Grid>

                <!-- Timeline Panel -->
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="2"/>
                        <RowDefinition Height="0.2*"/>
                        <RowDefinition Height="0.8*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#7F0E0E0E"/>

                    <StackPanel Grid.Row="1" Orientation="Horizontal">
                        <iconPacks:PackIconBootstrapIcons Margin="20, 0, 0, 0" Width="14" Height="14" VerticalAlignment="Center" Kind="StopwatchFill" Foreground="#FFB2BA"/>
                        <Label Content="TIMELINE" Margin="2, 0, 0, 0" Foreground="#E5E2E1" FontSize="14" FontWeight="Bold"  VerticalAlignment="Center"/>
                    </StackPanel>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="0.8*"/>
                            </Grid.RowDefinitions>

                            <Border  Grid.Row="0"  Background="#1C1B1B">
                                <Button Click="AddLayerButton_Click" Margin="10, 5, 10, 5" Height="30" Background="#353535" HorizontalContentAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Add Layer" Foreground="#FFBDBDBD" FontWeight="Medium" FontSize="14"  VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </Border>

                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">
                                <StackPanel x:Name="layersPanel" Background="#1C1B1B">

                                    <local:UC_LayerControl LayerType="Video" Margin="0, 10, 0, 0"/>
                                    <local:UC_LayerControl LayerType="Audio" Margin="0, 10, 0, 0"/>

                                </StackPanel>
                            </ScrollViewer>
                        </Grid>

                        <Grid Grid.Column="1" Background="#0E0E0E">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <Grid Grid.Row="0" Background="#0E0E0E">
                                <local:TimelineControl x:Name="timeline" />
                            </Grid>
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>

        <Grid Grid.Row="2">

        </Grid>
    </Grid>
</Window>
